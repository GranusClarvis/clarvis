# Clarvis — Docker profile
# Containerized dev/test setup. Stateless by design.
# This file is NOT used directly — Docker sets env via docker-compose.yml.
# Included for reference only.

# ── Workspace (set by Dockerfile) ────────────────────────────────────────
CLARVIS_WORKSPACE=/app

# ── Graph Backend ────────────────────────────────────────────────────────
CLARVIS_GRAPH_BACKEND=sqlite
