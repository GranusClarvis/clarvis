RESULT: success — All 3 wiki tasks completed, tested, and stored.

## Task Status

**1. [WIKI_PIPELINE_HOOKS]** — Done
- Created `scripts/wiki/wiki_hooks.py` with:
  - `postflight_wiki_ingest()` — promotion-gated auto-ingest from heartbeat postflight (only triggers for RESEARCH/WIKI task tags or output containing research markers)
  - `research_paper_to_wiki()` — bridges research_to_queue paper scanning to wiki source registry
  - `operator_drop()` — manual source drop (operator intent = gate)
  - `should_ingest_output()` — explicit promotion gate (tag match + output marker checks)
- Wired into `heartbeat_postflight.py` at §8.7 (after evolution synthesis, before digest)
- Wired into `research_to_queue.py` during `scan_papers()` (idempotent, fail-safe)
- Added `clarvis wiki drop <source>` CLI command with auto-compile

**2. [WIKI_NORMALIZE_SOURCE_PATHS]** — Done
- Added `check_source_paths()` to `wiki_lint.py` — detects:
  - Relative paths (`../`) in sources
  - Plural dirs (`raw/repos/`, `raw/papers/`)
  - Wiki cross-refs that belong in Related Pages
  - Invalid/unknown source types
  - Missing source files
- Registered as `source_paths` in `ALL_CHECKS`
- Verified: catches 21 real issues (4 errors, 17 warnings)

**3. [WIKI_RAW_DIR_MIGRATION]** — Done
- Moved `raw/repos/` → `raw/repo/` (1 file), removed empty `raw/papers/`, `raw/transcripts/`
- Fixed `wiki_ingest.py` lines 700, 796, 975, 1039 (repos→repo, papers→paper)
- Updated `sources.jsonl`, 2 wiki pages, schema docs, templates, tests, architecture doc
- Verified: 0 plural path lint issues remaining

**Brain**: 2 memories stored. **QUEUE.md**: 3 tasks marked `[x]`.

NEXT: [WIKI_TAG_TAXONOMY_ENFORCEMENT] and [WIKI_CONFIDENCE_AUTO_UPGRADE] are the natural follow-ups. The 17 remaining source_path warnings (mostly absolute-path cross-refs in sources) need a cleanup pass — consider a script to migrate them to `Related Pages`.

RESEARCH_RESULT:
  TOPIC: Wiki Pipeline Hooks Integration
  DECISION: APPLY
  FINDINGS: Wired wiki into postflight (promotion-gated), research_to_queue (paper bridge), and CLI (operator drop). Added source path lint check. Standardized raw dirs to singular names.
  RELEVANCE: Wiki is now triggered intentionally at 3 pipeline points rather than being a disconnected subsystem. Lint catches path drift early.
  QUEUE_ITEMS: none
  EFFORT: M
  STORED: 2 brain memories
