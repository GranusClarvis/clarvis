RESULT: success — Completed 2 tasks: WIKI_TAG_TAXONOMY_ENFORCEMENT and WIKI_CONFIDENCE_AUTO_UPGRADE

**Task 1: [WIKI_TAG_TAXONOMY_ENFORCEMENT]** ✓
- Added `normalize_tag()`, `normalize_tags()`, `is_taxonomy_tag()` functions to `wiki_compile.py` with:
  - 80+ keyword→category/topic mappings covering all 8 taxonomy categories
  - Passthrough for already-valid `category/topic` tags
  - Fallback to `uncategorized/{slug}` for unmapped concepts
- Applied normalization in all 3 page generators (`generate_repo_page`, `generate_paper_page`, `generate_concept_page`)
- Added `check_tag_taxonomy` lint check in `wiki_lint.py` that warns on:
  - Non-taxonomy tags (no `/` separator)
  - Unknown categories
  - Missing tags
  - Excessive tags (>5)
  - Uncategorized tags (info level)
- Registered in `ALL_CHECKS` dict so `--check tag_taxonomy` works

**Task 2: [WIKI_CONFIDENCE_AUTO_UPGRADE]** ✓
- Added `compute_confidence()` function: 0 sources=low, 1+=medium, 3+ verified=high
- Applied in all 3 page generators (replaces hardcoded `medium`)
- Applied in `update_existing_page()` — confidence recalculated when sources change

Files modified:
- `scripts/wiki/wiki_compile.py` — tag normalization + confidence auto-upgrade
- `scripts/wiki/wiki_lint.py` — tag taxonomy lint check
- `memory/evolution/QUEUE.md` — marked both tasks [x]

Tests: 12/12 normalize_tag, all compute_confidence, all is_taxonomy_tag passed. Lint runs successfully on real wiki (caught 13 non-taxonomy tags).

NEXT: [WIKI_SPINE_REFACTOR_PLAN] — design final subsystem layout for `clarvis/wiki/` consolidation.
